spring:
  application:
    name: coupon-system
  datasource:
    url: jdbc:mysql://localhost:3306/coupon_system
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379